-- c00lgui LIBRARY v4.4 | By Grok | 2025 | GUI GIGANTE + SCROLL TABS
-- loadstring(game:HttpGet("https://raw.githubusercontent.com/grok-ai/c00lgui-library/main/c00lgui_tabs_scroll.lua"))()

local c00lgui = {}
c00lgui.Version = "4.4"
c00lgui.Author = "Grok"
c00lgui.Keybind = Enum.KeyCode.Insert

local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local TweenService = game:GetService("TweenService")
local SoundService = game:GetService("SoundService")

-- Base (sem fundo)
local function CreateBase()
    local ScreenGui = Instance.new("ScreenGui")
    ScreenGui.Name = "c00lgui_GiantScroll"
    ScreenGui.Parent = LocalPlayer:WaitForChild("PlayerGui")
    ScreenGui.ResetOnSpawn = false
    ScreenGui.DisplayOrder = 999
    ScreenGui.IgnoreGuiInset = true
    return ScreenGui
end

function c00lgui:Create(title, callback)
    local ScreenGui = CreateBase()
    local MainFrame = Instance.new("Frame")
    MainFrame.Name = "MainFrame"
    MainFrame.Parent = ScreenGui
    MainFrame.BackgroundTransparency = 1
    MainFrame.BorderSizePixel = 0
    MainFrame.Position = UDim2.new(0.02, 0, 0.02, 0)
    MainFrame.Size = UDim2.new(0, 800, 0, 800)  -- GIGANTE
    MainFrame.Active = true
    MainFrame.Draggable = true
    MainFrame.Visible = false

    -- BORDA GLOW
    local Glow = Instance.new("UIStroke", MainFrame)
    Glow.Color = Color3.fromRGB(0, 255, 0)
    Glow.Thickness = 5
    Glow.Transparency = 0.3

    local Corner = Instance.new("UICorner", MainFrame)
    Corner.CornerRadius = UDim.new(0, 20)

    -- TÍTULO
    local Title = Instance.new("TextLabel")
    Title.Parent = MainFrame
    Title.BackgroundTransparency = 1
    Title.Size = UDim2.new(1, 0, 0, 80)
    Title.Font = Enum.Font.SourceSansBold
    Title.Text = "c00lgui | " .. title
    Title.TextColor3 = Color3.fromRGB(0, 255, 0)
    Title.TextScaled = true
    Title.TextStrokeTransparency = 0
    Title.TextStrokeColor3 = Color3.fromRGB(0, 0, 0)

    -- FECHAR
    local CloseBtn = Instance.new("TextButton")
    cartao
    CloseBtn.Parent = MainFrame
    CloseBtn.BackgroundColor3 = Color3.fromRGB(200, 0, 0)
    CloseBtn.BackgroundTransparency = 0.3
    CloseBtn.Position = UDim2.new(1, -80, 0, 15)
    CloseBtn.Size = UDim2.new(0, 70, 0, 55)
    CloseBtn.Font = Enum.Font.SourceSansBold
    CloseBtn.Text = "X"
    CloseBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
    CloseBtn.TextScaled = true
    local CloseCorner = Instance.new("UICorner", CloseBtn)
    CloseCorner.CornerRadius = UDim.new(0, 12)

    -- SCROLL PARA TABS (HORIZONTAL)
    local TabScroll = Instance.new("ScrollingFrame")
    TabScroll.Parent = MainFrame
    TabScroll.BackgroundTransparency = 1
    TabScroll.Position = UDim2.new(0, 25, 0, 100)
    TabScroll.Size = UDim2.new(1, -50, 0, 70)
    TabScroll.CanvasSize = UDim2.new(0, 0, 0, 0)
    TabScroll.ScrollBarThickness = 8
    TabScroll.ScrollBarImageColor3 = Color3.fromRGB(0, 255, 0)
    TabScroll.ScrollingDirection = Enum.ScrollingDirection.X

    local TabList = Instance.new("UIListLayout")
    TabList.Parent = TabScroll
    TabList.FillDirection = Enum.FillDirection.Horizontal
    TabList.Padding = UDim.new(0, 10)
    TabList.SortOrder = Enum.SortOrder.LayoutOrder

    -- Atualiza CanvasSize das TABS
    TabList:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(function()
        TabScroll.CanvasSize = UDim2.new(0, TabList.AbsoluteContentSize.X + 20, 0, 0)
    end)

    -- CONTEÚDO
    local ContentFrame = Instance.new("Frame")
    ContentFrame.Parent = MainFrame
    ContentFrame.BackgroundTransparency = 1
    ContentFrame.Position = UDim2.new(0, 25, 0, 180)
    ContentFrame.Size = UDim2.new(1, -50, 1, -205)

    local ui = {}
    local Tabs = {}
    local ActiveTab = nil

    function ui:Tab(name, callback)
        local TabBtn = Instance.new("TextButton")
        TabBtn.Parent = TabScroll
        TabBtn.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
        TabBtn.BackgroundTransparency = 0.5
        TabBtn.Size = UDim2.new(0, 150, 1, 0)  -- MAIOR
        TabBtn.Font = Enum.Font.SourceSansBold
        TabBtn.Text = name
        TabBtn.TextColor3 = Color3.fromRGB(180, 180, 180)
        TabBtn.TextScaled = true

        local TabCorner = Instance.new("UICorner", TabBtn)
        TabCorner.CornerRadius = UDim.new(0, 12)
        local TabStroke = Instance.new("UIStroke", TabBtn)
        TabStroke.Color = Color3.fromRGB(0, 255, 0)
        TabStroke.Thickness = 0

        local Scroll = Instance.new("ScrollingFrame")
        Scroll.Parent = ContentFrame
        Scroll.BackgroundTransparency = 1
        Scroll.Size = UDim2.new(1, 0, 1, 0)
        Scroll.CanvasSize = UDim2.new(0, 0, 0, 0)
        Scroll.ScrollBarThickness = 10
        Scroll.ScrollBarImageColor3 = Color3.fromRGB(0, 255, 0)
        Scroll.Visible = false

        local List = Instance.new("UIListLayout")
        List.Parent = Scroll
        List.Padding = UDim.new(0, 15)
        List:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(function()
            Scroll.CanvasSize = UDim2.new(0, 0, 0, List.AbsoluteContentSize.Y + 40)
        end)

        local tab = {}

        function tab:Toggle(name, default, callback)
            local state = default or false
            local Btn = Instance.new("TextButton")
            Btn.Parent = Scroll
            Btn.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
            Btn.BackgroundTransparency = 0.6
            Btn.Size = UDim2.new(1, 0, 0, 60)  -- MAIOR
            Btn.Font = Enum.Font.SourceSansBold
            Btn.Text = name .. " [OFF]"
            Btn.TextColor3 = Color3.fromRGB(255, 70, 70)
            Btn.TextScaled = true

            local Corner = Instance.new("UICorner", Btn)
            Corner.CornerRadius = UDim.new(0, 12)
            local Stroke = Instance.new("UIStroke", Btn)
            Stroke.Color = Color3.fromRGB(255, 70, 70)
            Stroke.Thickness = 2

            Btn.MouseButton1Click:Connect(function()
                state = not state
                Btn.Text = name .. (state and " [ON]" or " [OFF]")
                Btn.TextColor3 = state and Color3.fromRGB(0, 255, 0) or Color3.fromRGB(255, 70, 70)
                Stroke.Color = state and Color3.fromRGB(0, 255, 0) or Color3.fromRGB(255, 70, 70)
                pcall(callback, state)
                local s = Instance.new("Sound")
                s.SoundId = "rbxassetid://131961136"
                s.Volume = 0.3
                s.Parent = SoundService
                s:Play()
                s.Ended:Connect(function() s:Destroy() end)
            end)

            if default then task.spawn(function() Btn.MouseButton1Click:Fire() end) end
        end

        function tab:Button(name, callback)
            local Btn = Instance.new("TextButton")
            Btn.Parent = Scroll
            Btn.BackgroundColor3 = Color3.fromRGB(0, 140, 0)
            Btn.BackgroundTransparency = 0.4
            Btn.Size = UDim2.new(1, 0, 0, 55)
            Btn.Font = Enum.Font.SourceSansBold
            Btn.Text = name
            Btn.TextColor3 = Color3.fromRGB(255, 255,
